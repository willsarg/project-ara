// SPDX-License-Identifier: Apache-2.0
// Empty stand-in for the `server-only` package under vitest (see vitest.config.ts). The real
// package throws if imported outside a Server Component; in tests we import server modules directly.
export {};
